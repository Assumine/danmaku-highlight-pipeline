# 当前实现

## 1. 输入与录制

生产环境由 biliup 1.2.6 负责录像和 B 站弹幕 XML，录制端使用 `Noop` 上传。抖音弹幕由独立旁路采集器保存，旁路采集器不在本仓库中。算法只读取现有文件，不登录平台。

一场直播可能因为断流被拆成多个视频段。`dual_platform_program.py` 从 XML 评论时间戳建立每段录像的“视频时间 ↔ 绝对时间”映射。抖音评论先还原为绝对时间，再映射到实际存在的 B 站视频区间；落在无视频断流缺口内的评论丢弃。

## 2. UID 过滤

`danmaku_uid.py` 执行两个约束：

1. 缺失、`0`、`unknown` 等 UID 不参与独立用户证据。
2. 同一 UID 在 15 秒窗口内发送超过两条时，只保留首条和中间一条。

该策略用于降低刷屏对密度、用户数和主题证据的放大，但不会识别多人复制文案或跨账号协同行为。

## 3. 候选发现

`program_rule_engine_v2.py` 使用 24 秒窗口和 180 秒历史基线计算：

- 消息数与独立用户数
- 有效消息数
- 密度提升
- 新用户比例
- 笑声、反转、冲突、操作、翻车和疑问反应
- 高频复读和普通直播流程文本占比

候选必须同时满足群体性、相对历史基线的提升、有效笑声信号和最低评分。发现阶段默认向前预留 40 秒。

“老板糊涂、下播、录播、回归”等直播状态聊天只从复读证据中退出，不作为关键词黑名单直接否决候选；真实笑声和其他群体反应仍按原始证据计数。

## 4. 双平台融合

两边分别产生候选，时间区间重叠时合并证据；不重叠的单平台候选仍可保留。这个选择是为了减少漏剪，而不是把两个平台的尾部简单取最大值。

结尾使用 `stable_intervals` 的首次稳定回落，避免延迟弹幕或后续复读把视频尾部拖长。算法不会仅因较晚出现的笑声再延长到下一波反应。

首次回落确定后会重新验收候选：如果“笑点铺垫”的笑声回报已经落在最终结尾之外，该候选直接剔除。抖音单平台候选若同时只有最低两位笑声用户、没有多人笑声回报且密度提升低于 `2.5`，也会作为证据不足剔除；是否邻近另一个候选不改变这项判断。普通笑点即使总分达到 `9.0` 且有两位笑声用户，也必须额外满足至少 9 条有效弹幕、4 位反应用户或 3 位笑声用户之一，避免低样本窗口仅凭密度突增和两个弱笑声被误选。

## 5. 原节目单

`program_source_analysis.py` 独立于剪辑候选生成节目单。目前包含：

- 下饭坤：必须由 B 站笑声种子触发，抖音只能提供佐证。
- 理财、自私、内鬼、爽局：按多人关键词簇触发。
- 猎人模式、Boss/英雄模式：先以 60 秒切分局部簇，少于 3 位观众的小簇通常剔除；猎人模式允许至少 2 位观众的小簇在 210 秒内连接到多人强簇，真正孤立的小簇仍会剔除。

Boss/英雄模式默认要求至少持续 150 秒；猎人模式允许 120 秒以上的密集短簇。两者通常至少需要 6 条消息、4 位用户，且前后半段各至少 2 位用户，显示时间按首个受支持点向前预留 40 秒。猎人模式另允许至少 4 位用户持续 45 秒、其中至少 3 位讨论玩法或机制的短簇，并向前预留 90 秒，以覆盖观众延迟提问；单纯重复“窥屏猎人”不满足该规则。模式预测、想看和历史讨论不作当前命中，但不会否决随后出现的真实命中。Boss/英雄角色词只有在当前事件的多人证据下参与判定；多人即时角色证据也可构成短事件。爽局需要明确的主播受益表达和多人支持，剪辑候选从首个有效主题反馈向前保留 120 秒；“起飞”单独不作爽局证据。猎人、Boss/英雄和爽局的节目单标签均带“测试”后缀。

这些定义是当前直播间的运营标签，不是通用内容分类法。

## 6. 剪辑边界

`highlight_editor.py` 在候选点前保留固定铺垫，并可通过 faster-whisper 向前找到覆盖锚点的完整话轮。尾部先以稳定热度回落为锚点，再寻找当前句子的结束位置，最后可向附近音频弱谷轻微移动，减少切在爆音或半个音节上的情况。

片段合成支持视频交叉淡化和按边缘音量选择音频转场。最后一段额外做淡出保护。

## 7. 审计

每个候选记录触发时间、原始和修正结尾、平台证据、UID统计、语音文本、边界原因和输入文件摘要。`program_audit.py` 只读取这些记录，不修改素材。

## 8. 直播日汇总与稿件接口

直播日从 07:00 到次日 07:00。本地录制时间决定源 P 号与节目单 P 号，不以在线视频列表的当前顺序为准。投稿分 P 固定为全部无弹幕原片、全部有弹幕版本、下饭片段；每次同日新场次完成后重新生成一个当日汇总片段和一份节目单。仅当原片不可读时，节目单回退到按 P 分列。

本地处理完成后，调用 biliup 1.2.6 命令行创建或追加分 P。稿件编辑接口用于调整原片顺序和在新汇总片段转码完成后替换旧片段；每次编辑后必须重新读取线上 CID 顺序并核对。转码未完成时可先尝试重排，如果平台拒绝，则等待就绪再试。节目单评论发布前检查并删除本账号的旧节目单评论。

`biliup_integration.py` 提供命令和编辑载荷的纯函数；凭据、HTTP 调用、重试队列及媒体上传由部署方实现。接口细节见 [integration.md](integration.md)。
